version: "3.9"

services:
  codeforcer:
    container_name: codeforcer_python
    build:
      context: ../../backend/src
      dockerfile: ./Dockerfile
    command: >
      bash -c "pip install pytest && pytest /code/tests"
