version: "3.9"

services:
  codeforcer_project:
    container_name: codeforcer
    build:
      context: ../../backend/src
      dockerfile: ./Dockerfile
    command: >
      bash -c "pip install pytest && pytest backend/src/tests/"
