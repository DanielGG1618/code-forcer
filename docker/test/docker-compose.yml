version: "3.9"

services:
  codeforcer_project:
    image: codeforcer:test
    container_name: codeforcer
    build:
      context: ../../backend/src
    command: >
      bash -c "pip install pytest && pytest backend/src/tests/"
