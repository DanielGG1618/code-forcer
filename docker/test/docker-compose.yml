services:
  codeforcer:
    container_name: codeforcer_python
    build:
      context: ../../backend/src
      dockerfile: ./Dockerfile
    command: >
      bash -c "pip install pytest && pytest /tests"
