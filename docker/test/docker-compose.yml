services:
  codeforcer:
    container_name: codeforcer
    build:
      context: ../../backend/src
      dockerfile: ./Dockerfile
    command: >
      bash -c "pip install pytest && pytest code/tests/"
